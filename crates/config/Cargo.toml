[package]
name = "rust-rag-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
toml = "0.8"
rust-rag-error = { path = "../error" }
